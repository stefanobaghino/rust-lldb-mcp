{
  "name": "rust-lldb-mcp",
  "version": "0.1.0",
  "description": "On-demand LLDB orchestrator exposed as an MCP server for Claude Code.",
  "type": "module",
  "bin": {
    "rust-lldb-mcp": "index.js"
  },
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "biome ci .",
    "format": "biome format --write .",
    "test": "node --test test/*.test.js",
    "smoke": "node smoke-test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.13"
  }
}
